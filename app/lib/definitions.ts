export type FoundForm = {
  firstName: string;
  lastName: string;
  cardNumber: string;
  category: string;
  collectionPoint: string;
  region: string;
  id: string;
};

export type Item = {};
